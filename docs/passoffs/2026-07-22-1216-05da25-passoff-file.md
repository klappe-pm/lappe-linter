---
domain: development
category: obsidian-linter-fork
sub-category: passoff
date-created: 2026-07-22
date-revised: 2026-07-22
status: REVIEW
aliases: []
tags:
  - passoff
  - lappe-linter
---
# 2026-07-22-1216-05da25-passoff-file

## Summary

Audited lappe-linter master against the recovered UI spec (three adversarial agents): all five named features are present in code and spec-aligned, and the prior YAML autocomplete downgrade is genuinely restored. Shipped code PR #21 (deleted the dead `LappePreviewModal`, added an underscore example to the preview sample; build + 1844 tests green). Deployed a corrected, all-kebab `linter.yaml` control file to 10 `Projects/*` vaults via per-repo branch PRs, stripped the leftover `linterConfigPreview` review blob from four `data.json` files, and amended `~/.claude/rules/common/markdown-style.md` to pure kebab-case headings. Two mid-session errors (committing config to `main` instead of a branch; assuming all-kebab applied fleet-wide) were caught by advisor review and fully corrected before close.

## Inherited state

### Open PRs

- #21 refactor(ui): delete dead LappePreviewModal, enrich preview sample | `claude/session-20260722-073524` | ready | age 0d | https://github.com/klappe-pm/lappe-linter/pull/21 (this session, code)
- #22 chore: add lappe-linter config (kebab-case headings) | `chore/lappe-linter-config` | ready | age 0d | https://github.com/klappe-pm/lappe-linter/pull/22 (this session, config)
- Fleet config PRs opened this session (one per repo): workflows #11, jobs #102, harness #811, green-lappe-properties #83, session-data #261, regulatory-ingredients-labels-laws #29, legal-tbi #8, product-management #39.

### Carry-forward from prior passoffs

- [2026-07-21-1741] Recovered UI spec is authoritative; there is no standalone config view and no "Lappe" tab. Honored this session: preview kept as the live side-by-side `LappePreviewView`; no Lappe/Style tab reintroduced.

### Prior passoff chain

- 2026-07-21-1741 (revert bolted-on config surface, recover UI spec)

## Files changed

Created:
- `docs/passoffs/2026-07-22-1216-05da25-passoff-file.md`
- `linter.yaml` (active untracked file) in 10 `Projects/*` vaults; committed on branch PRs in 9 git repos.

Modified (separate PRs/commits, not on this passoff branch):
- `src/lappe/preview-sample.ts` (underscore example plus comment fix), PR #21.
- `~/.claude/rules/common/markdown-style.md` (kebab headings), committed `eadd88d` on `~/.claude` main.
- 4 `data.json` files: removed `linterConfigPreview` blob (local plugin state, uncommitted).

Deleted:
- `src/ui/modals/lappe-preview-modal.ts` (dead code), PR #21.

## Key decisions

- Preview stays the live side-by-side `LappePreviewView`, not the plan's example-note button: the recovered spec and the user's own words ("did you build the side by side view?") outrank the plan's downgrade.
- Heading case is pure kebab everywhere (user directive); `markdown-style.md` amended to match, dropping the Title-Case exceptions.
- Config carried only parser-validated rule ids (header-case, yaml-key-sort, yaml-timestamp, alphabetize, h1-matches-stem, kebab-case-filename, paragraph-spacing, list-style, join-paragraph-lines) to avoid the fail-closed loader.
- Config committed via per-repo branch plus PR (not main) so nothing advances shared `main`; the active untracked file keeps the linter working pre-merge.

## Risks and open questions

- **`skill-chaining` breakage:** pure-kebab linting rewrites `## Chaining Interface` / `## Next Actions` to kebab, breaking that literal-heading matcher. The matcher must be updated (or those files excluded from header-case) before linting harness skill docs. Flagged in the amended rule; NOT yet fixed.
- **Task 2 (Obsidian visual verification) is unverified.** Build is installed in `~/Documents/lappe-linter-test`; the six tabs must be confirmed rendering in the GUI. Do not mark UI done from tests or code alone.
- Real feature gaps remain (need GUI verify loops): callouts missing from Special formatting plus no callout core rule; Basic Styling lacks a standalone Underscore control and labels Bold/Italics with upstream names; `remove-yaml-keys` uses a textarea not drag-rows; `links`/`preset` not first-class scope types.
- `linterConfigPreview` blob deleted after only a partial carry; the on-disk record of the old PM config is gone (still in this session's transcript).
- `~/.claude` commit `eadd88d` is local/unpushed; push via the normal `~/.claude` flow.

## Workspace sweep

### Auto-resolved

- _none_ beyond this session's own documented work.

### Needs user action

- `Untitled.md` at lappe-linter repo root is a misplaced **harness** resume note (PR #797/#793 content); move or delete it, since it does not belong in lappe-linter.
- `Untitled.base` (empty) and `.DS_Store` files at lappe-linter root are Obsidian cruft; gitignore or remove.
- `README.md` in lappe-linter primary tree is a full uncommitted rewrite; review and commit or discard.
- Nine `chore/lappe-linter-config` branches (plus product-management) are open PRs awaiting review/merge; each vault also holds an untracked active `linter.yaml`.

## Lessons learned

- Rigor: high, obligations met (4 epic-boundary checkpoints recorded; 3-agent independent audit; 1844 tests green). Two defects slipped past self-review and were caught only by advisor: (1) committed config to `main` when the user selected "on a branch"; (2) assumed all-kebab applied fleet-wide, contradicting the user's own `markdown-style.md`. Calibration takeaway: for multi-repo deploys, treat "where does this apply" and "which ref do I commit to" as explicit user decisions, not defaults, and run the independent review BEFORE the deploy, not after.

## State

- **Branch:** passoff/2026-07-22-1216
- **Commit:** 364dcc3
- **PR:** https://github.com/klappe-pm/lappe-linter/pull/23
- **Passoff version:** v1

## Next actions

1. Confirm in Obsidian (test vault `~/Documents/lappe-linter-test`) that the six settings tabs render; toggle the plugin off then on first. Only then mark the UI audit items done.
2. Review and merge the fleet config PRs (#8, #11, #22, #29, #39, #83, #102, #261, #811) and code PR #21.
3. Reconcile the `skill-chaining` heading matcher (accept kebab heading names, or exclude those files from header-case) before linting harness skill docs; push `~/.claude` `eadd88d`.
4. Build the callout core rule (pure logic, fully testable without the GUI) as the next feature PR, then wire it into Special formatting.
5. Address remaining feature gaps one GUI-verified PR at a time: Basic Styling Underscore control plus Bold/Italics relabel; `remove-yaml-keys` drag-row pattern; `links`/`preset` scope types.
6. Triage the lappe-linter dirty tree (Untitled.md misplaced harness note, README rewrite, .DS_Store).

## Do not do

- Do not reintroduce a "Lappe" tab, a combined "Style" tab, a standalone config view, or the reverted PM preview engine.
- Do not rip out the live side-by-side `LappePreviewView` to match the plan's example-note downgrade.
- Do not mark UI/settings items done from tests or code alone; the prior session's failure was exactly that.
- Do not lint harness skill docs with the kebab header-case rule until the `skill-chaining` matcher is reconciled.

## Next-session prompt

Copy-pasteable prompt for the next session:

```
cd ~/Projects/lappe-linter
/resume

Continue the lappe-linter recovered-UI-spec work. Context from the 2026-07-22 passoff
(merged PR #23): the audit is done, code PR #21 and 9 fleet config PRs are open, the
all-kebab linter.yaml is active (untracked) in every Projects/* vault, and
markdown-style.md was amended to pure-kebab headings (~/.claude commit eadd88d, unpushed).

Do these in order:
1. FIRST, in Obsidian on ~/Documents/lappe-linter-test: toggle the plugin off then on and
   confirm the six settings tabs (YAML, Headers, Body, Special formatting, Scopes, Rule
   order) render. Do NOT mark any UI item done from tests/code alone, that was the prior
   failure mode. Report what you actually see.
2. Reconcile the skill-chaining heading matcher (## Chaining Interface / ## Next Actions)
   for kebab headings, OR exclude those files from header-case, BEFORE any harness skill
   docs get linted. Then push ~/.claude eadd88d.
3. Review/merge the open config PRs (#8 #11 #22 #29 #39 #83 #102 #261 #811) and code PR #21.
4. Build the callout core rule (pure logic, fully testable without the GUI) as the next
   feature PR, then wire it into the Special formatting tab.
5. Then, one GUI-verified PR at a time: Basic Styling Underscore control + Bold/Italics
   relabel; remove-yaml-keys drag-row pattern; links/preset first-class scope types.

Do NOT reintroduce a Lappe tab, Style tab, standalone config view, or the reverted PM
preview engine. Keep the live side-by-side LappePreviewView (it matches the recovered spec).
Do substantive code work in a linked worktree, not the primary tree.
```
