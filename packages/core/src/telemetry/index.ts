/**
 * Telemetry event contract (WS-D). Pure and Obsidian-free: the CLI, the
 * plugin, and any hook emit these identical shapes so session-data ingests one
 * schema regardless of surface.
 */
export * from './events';
